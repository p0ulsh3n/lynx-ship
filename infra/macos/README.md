# macOS worker

Xcode and iOS signing run on macOS hosts. They are not replaced with a generic Linux Docker image. The worker implementation is Planned until cleanup, keychain isolation and provenance tests exist.
