# Docker

Container build and local self-hosting assets live here as the deployment surface is split out of application packages. The root `Dockerfile` remains the current build entrypoint during migration.
