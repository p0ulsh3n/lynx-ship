# Android worker

Status: Planned. The target is a Linux/Node worker that invokes the pinned Rspeedy, JDK, Android SDK and Gradle toolchain. No fake build implementation is exposed until the isolation and toolchain acceptance tests exist.
